fastapi>=0.110
uvicorn[standard]>=0.27
SQLAlchemy>=2.0
alembic>=1.13            # schema changes; runs as a pre-deploy step, not at startup
pydantic>=2.6
httpx>=0.27
python-dateutil>=2.9
PyJWT>=2.8
psycopg2-binary>=2.9   # Postgres driver; not needed for the SQLite default
sentry-sdk[fastapi]>=2.0    # optional; only activates when SENTRY_DSN is set
